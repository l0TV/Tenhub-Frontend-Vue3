# 开发环境中 Gateway 的地址。生产环境通过同源 /api 访问时可留空。
VITE_GATEWAY_URL=http://localhost:88

# 可选：覆盖 API 前缀，分类和搜索服务默认都通过 /api 访问。
# VITE_API_BASE_URL=/api

# 可选：覆盖智能客服（ai-customer-service）地址。默认取 ${VITE_API_BASE_URL}/ai，
# 即经网关访问 http://localhost:88/api/ai。分别部署 AI 服务时可指向其独立域名。
# VITE_AI_BASE_URL=/api/ai
